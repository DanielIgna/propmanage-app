[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "emergentintegrations"
version = "0.1.0"
description = "Local dev stub for Emergent integrations (LLM + Stripe wrappers)"

[tool.setuptools.packages.find]
where = ["src"]
